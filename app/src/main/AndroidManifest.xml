<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wh2mi.pelangi.wh2midummy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HalamanDepan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InputGejala" />
        <!-- <activity android:name=".HalamanDepan"></activity> -->
        <activity android:name=".InputKondisiLingkungan" />
        <activity android:name=".HasilDiagnosa" />
        <activity android:name=".cardViewDiagnosa" />
        <activity android:name=".InfoPenyakit" />
        <activity android:name=".SaranPenanganan" />
        <activity android:name=".SaranPencegahan"></activity>
    </application>

</manifest>